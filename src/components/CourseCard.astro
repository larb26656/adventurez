---
interface Props {
  title: string;
  tag?: string;
  thumbnail?: string;
  href: string;
  level?: string;
  duration?: string;
  rating?: string;
  price?: string;
}

const { 
  title, 
  tag = "Adventure", 
  thumbnail,
  href, 
  level = "Beginner", 
  duration = "3 hrs", 
  rating = "★ 4.8",
  price = "Free"
} = Astro.props;
---

<a class="course-card" href={href}>
  <div class="course-thumb">
    {thumbnail ? (
      <img src={thumbnail} alt={title} class="course-thumbnail-img" />
    ) : (
      <div class="pixel-art">░░██░░\n░░██░░\n░████░\n░░██░░\n░░██░░</div>
    )}
  </div>
  <div class="course-body">
    <div class="course-tag">{tag}</div>
    <div class="course-title">{title}</div>
    <div class="course-meta">
      <span>{level}</span>
      <span>{duration}</span>
    </div>
    <div class="course-footer">
      <div class="course-free">{price}</div>
      <button class="course-btn">Start</button>
    </div>
  </div>
</a>