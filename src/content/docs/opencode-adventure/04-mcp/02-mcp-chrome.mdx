---
title: Chrome DevTools MCP
description: ใช้ AI ควบคุม Chrome browser โดยตรง
order: 3
---

Chrome DevTools MCP คือ MCP server ที่พัฒนาโดย Google ที่จะนำความสามารถของ Chrome DevTools มาสู่ AI agent

## ทำไมต้องใช้ Chrome DevTools MCP

เอเจนต์ AI เผชิญปัญหาพื้นฐานคือไม่สามารถดูสิ่งที่โค้ดทำได้จริงเมื่อรันในเบราว์เซอร์ Chrome DevTools MCP ช่วยให้ AI สามารถ:

- ยืนยันการเปลี่ยนแปลงโค้ดแบบเรียลไทม์
- วินิจฉัยข้อผิดพลาดเกี่ยวกับเครือข่ายและคอนโซล
- จำลองพฤติกรรมของผู้ใช้ (คลิก กรอกฟอร์ม)
- แก้ไขข้อบกพร่องเกี่ยวกับ CSS และ layout
- ตรวจสอบประสิทธิภาพ (LCP, INP, CLS)

## ติดตั้ง Chrome DevTools MCP

เพิ่ม config ต่อไปนี้ใน `opencode.jsonc`:

```jsonc
{
  "mcp": {
    "chrome-devtools": {
      "type": "local",
      "command": ["npx", "-y", "chrome-devtools-mcp@latest"],
      "enabled": true
    }
  }
}
```

## Tools ที่มีให้ใช้

| Tool | คำอธิบาย |
|------|----------|
| `navigate_page` | ไปยัง URL หรือ back/forward/reload |
| `take_snapshot` | ดึง DOM tree ของหน้าปัจจุบัน |
| `take_screenshot` | ถ่ายภาพหน้าจอ |
| `click` | คลิกบน element |
| `fill` | พิมพ์ข้อความใน input |
| `evaluate_script` | รัน JavaScript |
| `performance_start_trace` | เริ่มบันทึก performance trace |
| `performance_stop_trace` | หยุดบันทึก |
| `list_network_requests` | ดู network requests |
| `get_console_messages` | ดู console logs |
| `lighthouse_audit` | รัน Lighthouse audit |

## ตัวอย่างการใช้งาน

```
Check the LCP of localhost:8080
```

```
Why does submitting the form fail after entering an email address?
```

```
The page looks strange. Check what's happening there.
```

## ข้อกำหนดเบื้องต้น

- Chrome browser ต้องติดตั้งบนเครื่อง
- Chrome ต้องเปิดอยู่ (MCP จะเชื่อมต่อผ่าน Chrome DevTools Protocol)

import { Quiz } from "@/components/quiz/Quiz"

export const quizMcpChrome = {
  id: "mcp-chrome-quiz",
  title: "ทดสอบความเข้าใจเกี่ยวกับ Chrome DevTools MCP",
  questions: [
    {
      id: "q1",
      type: "single",
      question: "Chrome DevTools MCP พัฒนาโดยใคร?",
      options: ["Facebook", "Google", "Microsoft", "Apple"],
      correctAnswer: "Google",
      points: 1
    },
    {
      id: "q2",
      type: "single",
      question: "Chrome DevTools MCP ช่วยให้ AI สามารถทำอะไรได้?",
      options: ["รัน JavaScript ใน server", "ควบคุม Chrome browser โดยตรง", "เขียนโค้ดแทน", "deploy เว็บไซต์"],
      correctAnswer: "ควบคุม Chrome browser โดยตรง",
      points: 1
    },
    {
      id: "q3",
      type: "multiple",
      question: "Chrome DevTools MCP มี tools ให้ใช้ทำอะไรได้บ้าง?",
      options: ["ถ่ายภาพหน้าจอ", "ดึง DOM tree", "รัน JavaScript", "ส่งอีเมล"],
      correctAnswer: ["ถ่ายภาพหน้าจอ", "ดึง DOM tree", "รัน JavaScript"],
      points: 1
    },
    {
      id: "q4",
      type: "single",
      question: "ข้อกำหนดเบื้องต้นของ Chrome DevTools MCP คืออะไร?",
      options: ["ต้องติดตั้ง Firefox", "Chrome ต้องเปิดอยู่", "ต้องมี internet", "ต้องใช้ Windows เท่านั้น"],
      correctAnswer: "Chrome ต้องเปิดอยู่",
      points: 1
    }
  ]
}

<Quiz quiz={quizMcpChrome} client:load />