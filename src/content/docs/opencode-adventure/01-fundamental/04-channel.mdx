---
title: Channel
description: ช่องทางการใช้งาน Opencode
order: 4
---

import ChatMessage from "@/components/ChatMessage.astro"

## Channel

Opencode มีช่องทางการใช้งานหลายรูปแบบ:

### TUI

**Text-based User Interface** คือการใช้งานผ่าน terminal/command line

**ข้อดี:**
- เร็วและเบา
- ไม่ต้องเปิด browser
- เหมาะกับ developer ที่ชอบใช้ terminal

**วิธีใช้:**
```bash
opencode
# พิมพ์คำถามหรือคำสั่งที่นี่
```

### Web

**Web Interface** คือการใช้งานผ่านเว็บไซต์

**ข้อดี:**
- สวยงาม ใช้งานง่าย
- มี UI สำหรับจัดการ conversation
- เหมาะกับผู้ที่ไม่ถนัด terminal

**วิธีใช้:**
```bash
opencode web
```

<ChatMessage message="เลือกช่องทางที่ถนัดได้เลย ไม่ว่าจะเป็น TUI หรือ Web!" avatarKey="whiteCat" />

## Scope ของ Opencode

Opencode ทำงานใน **working directory** ที่คุณเปิด terminal ณ ตอนนั้น หมายความว่า:

- **เข้าถึงได้เฉพาะไฟล์ใน directory นั้น** - Opencode จะเห็นและทำงานกับไฟล์ที่อยู่ใน directory ปัจจุบันและ subdirectory ของมัน
- **ไม่สามารถอ่านไฟล์ข้างนอก directory** - โดยค่าเริ่มต้น Opencode จะจำกัด scope ไว้เพื่อความปลอดภัย

**กรณีต้องการเข้าถึงไฟล์นอก directory:**

หากคุณต้องการให้ Opencode ทำงานกับไฟล์ที่อยู่นอก working directory จะมีข้อความแจ้งเตือนขอ permission ก่อน และเมื่ออนุญาตแล้ว Opencode จะสามารถเข้าถึงไฟล์ใน path ที่ระบุได้

<ChatMessage message="ถ้าต้องการทำงานกับไฟล์ที่อยู่นอก directory ก็แค่ขอ permission ผ่านข้อความยืนยัน" avatarKey="whiteCat" />

import { Quiz } from "@/components/quiz/Quiz"

export const quiz4 = {
  id: "channel-quiz",
  title: "ทดสอบความเข้าใจเกี่ยวกับ Channel และ Scope",
  questions: [
    {
      id: "q1",
      type: "single",
      question: "TUI ย่อมาจากอะไร?",
      options: ["Text User Interface", "Terminal User Interface", "Text-based User Interface", "Terminal Universal Interface"],
      correctAnswer: "Text-based User Interface",
      points: 1
    },
    {
      id: "q2",
      type: "multiple",
      question: "ข้อดีของ TUI มีอะไรบ้าง?",
      options: ["เร็วและเบา", "ต้องเปิด browser", "ไม่ต้องเปิด browser", "เหมาะกับ developer ที่ชอบใช้ terminal"],
      correctAnswer: ["เร็วและเบา", "ไม่ต้องเปิด browser", "เหมาะกับ developer ที่ชอบใช้ terminal"],
      points: 1
    },
    {
      id: "q3",
      type: "single",
      question: "คำสั่งใดใช้เปิด Web Interface?",
      options: ["opencode", "opencode web", "opencode ui", "opencode browser"],
      correctAnswer: "opencode web",
      points: 1
    },
    {
      id: "q4",
      type: "single",
      question: "Opencode ทำงานใน scope ใดเป็นหลัก?",
      options: ["ทั้งระบบ", "เฉพาะ working directory", "เฉพาะ home directory", "เฉพาะ project ที่เลือก"],
      correctAnswer: "เฉพาะ working directory",
      points: 1
    }
  ]
}

<Quiz quiz={quiz4} client:load />