---
title: "Hooks"
description: เรียนรู้ Hooks พื้นฐาน — useState, useEffect, useRef, useMemo และ Styling
---
import ChatMessage from "@/components/ChatMessage.astro"

## Hooks คืออะไร?

Hook คือ **function พิเศษ** ที่ให้คุณ "ต่อ" กับ React features ต่าง ๆ เช่น state, lifecycle, refs

```mermaid
flowchart TB
    subgraph Hooks
        useState[useState<br/>State management]
        useEffect[useEffect<br/>Side effects]
        useRef[useRef<br/>DOM access]
        useMemo[useMemo<br/>Memoization]
    end
    
    classDef hook fill:#a78bfa,stroke:#7c3aed,stroke-width:2px
    class useState,useEffect,useRef,useMemo hook
```

<ChatMessage message="ทุก Hook ขึ้นต้นด้วย 'use' — เป็น convention ของ React เลยนะ" avatarKey="whiteCat" />

---